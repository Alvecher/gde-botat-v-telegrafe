# Сырые выгрузки расписания и сама таблица: содержат ФИО преподавателей.
# data/last-good.json не игнорируется: там только числа (сколько строк и
# занятий было в прошлой удачной выгрузке), это нужно CI между запусками.
data/raw/
data/occupancy.json
data/last-good.xlsx
data/download-new.xlsx
data/*.part

# Собранные страницы: генерируются build_site.py при каждой сборке,
# в CI собираются заново и публикуются как артефакт Pages, без коммита.
site/index.html
site/floors.html
